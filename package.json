{
	"name": "p2ce-panorama",
	"version": "1.0.1",
	"description": "P2:CE's default Panorama UI.",
	"author": "P2:CE & Momentum Team",
	"license": "MIT",

	"scripts": {
		"watch": "babel scripts/ --extensions .ts,.js --watch --out-dir .jsbuild/",
		"build": "babel scripts/ --extensions .ts,.js --out-dir .jsbuild/",
		"lint": "eslint",
		"fix": "eslint --fix",
		"postinstall": "husky install",
		"pre-commit": "lint-staged"
	},

	"dependencies": {},
	"devDependencies": {
		"typescript": "^5.1.6",
		
		"@babel/cli": "^7.22.9",
		"@babel/core": "^7.22.9",
		"@babel/preset-env": "^7.22.9",
		"@babel/preset-typescript": "^7.22.5",

		"eslint": "^8.45.0",
		"eslint-config-prettier": "^8.5.0",
		"eslint-plugin-prettier": "^4.2.1",

		"husky": "^8.0.1",
		"lint-staged": "^12.4.1",

		"@prettier/plugin-xml": "^2.1.0",
		"prettier": "^2.6.2"
	},

	"lint-staged": {
		"**/*.{ts,xml,scss,css,md}": "eslint --fix",
		"scripts/**/*.ts": "babel scripts/ --extensions .ts,.js --out-dir .jsbuild/"
	}
}
